<Window x:Class="RentalCarApplication.View.CompletedOrderPhotosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Фото завершенного заказа"
        Width="940"
        Height="560"
        MinWidth="820"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border Background="WhiteSmoke" CornerRadius="22" BorderBrush="{DynamicResource PrimaryHueDarkBrush}" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="74"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="20,20,0,0">
                <Grid Margin="24,0,24,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Фото завершенного заказа" Foreground="White" FontSize="19"/>
                        <TextBlock Text="{Binding HeaderText}" Foreground="WhiteSmoke" FontSize="12" Margin="0,3,0,0"/>
                    </StackPanel>

                    <Button Width="40"
                                Height="40"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,0,0"
                                Background="White"
                                Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="CloseButton_Click"
                                Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,16,18,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,8,0" Background="White" CornerRadius="18" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Спереди" FontSize="16" FontWeight="Medium" HorizontalAlignment="Center"/>
                        <Image Source="{Binding FrontPhotoPath}" Height="290" Stretch="Uniform" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="4,0" Background="White" CornerRadius="18" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Сзади" FontSize="16" FontWeight="Medium" HorizontalAlignment="Center"/>
                        <Image Source="{Binding RearPhotoPath}" Height="290" Stretch="Uniform" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Margin="8,0,0,0" Background="White" CornerRadius="18" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Сбоку" FontSize="16" FontWeight="Medium" HorizontalAlignment="Center"/>
                        <Image Source="{Binding SidePhotoPath}" Height="290" Stretch="Uniform" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Padding="14" Background="WhiteSmoke" CornerRadius="0,0,20,20">
                <Button  Width="150"
 Height="40"
                        HorizontalAlignment="Center"
                        Click="CloseButton_Click"
                        Style="{StaticResource MaterialDesignRaisedDarkButton}"
                        materialDesign:ButtonAssist.CornerRadius="12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Закрыть"/>
                    </StackPanel>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
