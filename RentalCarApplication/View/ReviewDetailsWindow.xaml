<Window x:Class="RentalCarApplication.View.ReviewDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Отзыв"
        Width="760"
        MinHeight="360"
        MaxHeight="720"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">
    <Border Background="WhiteSmoke" CornerRadius="24" BorderBrush="{DynamicResource PrimaryHueMidBrush}" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="72"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="22,22,0,0">
                <Grid>
                    <TextBlock Text="Карточка отзыва"
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Medium"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    <Button Width="44"
                            Height="44"
                            Margin="0 0 18 0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="CloseButton_Click"
                            Background="White"
                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                            BorderBrush="{x:Null}"
                            Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20 18 20 20">
                <Border Padding="18" CornerRadius="18" BorderBrush="Gainsboro" BorderThickness="1" Background="White">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock x:Name="txtAuthor"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="DarkSlateGray"
                                       DockPanel.Dock="Left"/>
                            <TextBlock x:Name="txtCreatedAt"
                                       HorizontalAlignment="Right"
                                       Foreground="Gray"
                                       FontSize="14"/>
                        </DockPanel>

                        <TextBlock x:Name="txtCar"
                                   FontSize="15"
                                   Foreground="DimGray"
                                   Margin="0 6 0 0"/>

                        <TextBlock x:Name="txtRating"
                                   FontSize="24"
                                   Foreground="Goldenrod"
                                   Margin="0 12 0 0"/>

                        <TextBlock x:Name="txtEmail"
                                   FontSize="14"
                                   Foreground="DimGray"
                                   Margin="0 6 0 0"/>

                        <TextBlock Text="Текст отзыва"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0 18 0 8"
                                   Foreground="DarkSlateGray"/>

                        <TextBlock x:Name="txtReviewText"
                                   FontSize="16"
                                   TextWrapping="Wrap"
                                   Foreground="#333333"/>

                        <Image x:Name="imgPhoto"
                               Height="220"
                               Stretch="Uniform"
                               Margin="0 18 0 0"/>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            
        </Grid>
    </Border>
</Window>
